"""{{ package_name }} - {{ description }}

Built with AI assistance under human review. Zero dependencies by design:
audit this module, that is the whole supply chain.
"""


def hello() -> str:
    return "{{ package_name }}"
